This is a very naive implementation of an audio player that loads a play list from a remote URL.

It is used in one of our internal projects so we have control of the audio formatting etc so the client side is just concatenating a bunch of media files rather than muxing them properly.
